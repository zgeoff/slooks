name: Main

on:
  push:
    branches: [main]
  # Recovery path for a release that tagged but failed to publish: skips the
  # release-please steps and re-runs only the publish steps. Already-published
  # versions and already-uploaded assets are replaced or skipped, so
  # re-running is safe.
  workflow_dispatch:
    inputs:
      republish_paths:
        description: 'JSON array of package dirs to republish, e.g. ["."]'
        required: true
        type: string

permissions:
  actions: read
  contents: read

concurrency:
  group: main-checks
  cancel-in-progress: true

jobs:
  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: .bun-version

      - name: Install
        run: bun install --frozen-lockfile

      - name: Audit Dependencies
        run: bun run audit

      - name: Typecheck
        run: bun run typecheck

      - name: Test
        run: bun test

  binaries:
    uses: ./.github/workflows/binaries.yml

  release:
    needs: [checks, binaries]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    steps:
      # A release takes two runs of this job:
      # - Run 1, after a releasable commit lands on main: release-please
      #   opens or updates the release PR (version bump + changelog), the
      #   sync step makes that PR pass the checks job, and auto-merge merges
      #   it.
      # - Run 2, triggered by that merge: release-please turns the merged
      #   bump into a tag + GitHub release, then the binaries, the Homebrew
      #   formula, and the npm package ship in that order.
      #
      # The App token exists because GITHUB_TOKEN events don't trigger
      # workflows: a GITHUB_TOKEN merge of the release PR would never start
      # run 2. With no App configured (see RELEASING.md), release PRs are
      # left open to merge by hand and the chain continues from there.
      - name: Mint GitHub App token
        id: app-token
        if: github.event_name == 'push' && vars.RELEASE_APP_ID != ''
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}

      - name: Run release-please
        id: release
        if: github.event_name == 'push'
        uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 # v5.0.0
        with:
          token: ${{ steps.app-token.outputs.token || github.token }}

      # The release PR bumps the package.json version that bun.lock also
      # records; sync it so the PR passes the frozen-lockfile install.
      - name: Check out release PR
        if: steps.release.outputs.prs_created == 'true'
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: release-please--branches--main--components--@zgeoff/slooks
          token: ${{ steps.app-token.outputs.token || github.token }}
      - name: Set up bun
        if: steps.release.outputs.prs_created == 'true'
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: .bun-version
      - name: Sync lockfile on release PR
        if: steps.release.outputs.prs_created == 'true'
        run: |
          bun install
          if ! git diff --quiet; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git commit -am "chore: sync lockfile for release"
            git push
          fi

      # Reads no release-please outputs on purpose: prs_created is false
      # when the open release PR needed no update (a previous run failed
      # after creating it). Mergeability is recomputed asynchronously after
      # the sync push, and a MERGEABLE read can be stale, so the merge
      # attempt retries inside the same loop.
      - name: Auto-merge release PRs
        if: github.event_name == 'push' && steps.app-token.outputs.token != ''
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          for pr in $(gh pr list --repo "$GITHUB_REPOSITORY" --head "release-please--branches--main--components--@zgeoff/slooks" --json number --jq '.[].number'); do
            merged=''
            for _ in $(seq 1 12); do
              state=$(gh pr view "$pr" --repo "$GITHUB_REPOSITORY" --json mergeable --jq .mergeable)
              if [ "$state" = "CONFLICTING" ]; then
                echo "release PR #$pr has conflicts" >&2
                exit 1
              fi
              if [ "$state" = "MERGEABLE" ] && gh pr merge "$pr" --repo "$GITHUB_REPOSITORY" --squash; then
                merged=1
                break
              fi
              sleep 5
            done
            if [ -z "$merged" ]; then
              echo "release PR #$pr never became mergeable" >&2
              exit 1
            fi
          done

      - name: Check out merged release
        if: steps.release.outputs.releases_created == 'true' || inputs.republish_paths != ''
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Download binaries
        if: steps.release.outputs.releases_created == 'true' || inputs.republish_paths != ''
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: binaries
          path: dist
      # --clobber makes a re-run replace assets a failed run left behind.
      - name: Attach binaries to the release
        if: steps.release.outputs.releases_created == 'true' || inputs.republish_paths != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version=$(jq -r .version package.json)
          gh release upload "@zgeoff/slooks@$version" dist/slooks-* dist/SHA256SUMS --clobber --repo "$GITHUB_REPOSITORY"

      # The tap is a second repository, so the release App has to be
      # installed on it as well and HOMEBREW_TAP set to its name (see
      # RELEASING.md). With either missing, the formula is left alone.
      - name: Mint tap token
        id: tap-token
        if:
          (steps.release.outputs.releases_created == 'true' || inputs.republish_paths != '') &&
          vars.RELEASE_APP_ID != '' && vars.HOMEBREW_TAP != ''
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ vars.HOMEBREW_TAP }}
      - name: Update the Homebrew formula
        if: steps.tap-token.outputs.token != ''
        env:
          GH_TOKEN: ${{ steps.tap-token.outputs.token }}
          TAP: ${{ github.repository_owner }}/${{ vars.HOMEBREW_TAP }}
        run: |
          version=$(jq -r .version package.json)
          git clone "https://x-access-token:$GH_TOKEN@github.com/$TAP.git" tap
          mkdir -p tap/Formula
          scripts/build-formula.sh "$version" dist/SHA256SUMS > tap/Formula/slooks.rb
          cd tap
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Formula/slooks.rb
          git commit -m "slooks $version" || echo "formula already at $version"
          git push

      # npm ships last so a publish failure (the trusted publisher not yet
      # configured, see RELEASING.md) never blocks the binaries or the tap.
      # npm does the publishing, not bun: trusted publishing (OIDC) needs
      # npm >= 11.5.1, which node 24 bundles, and bun publish has no OIDC
      # support (oven-sh/bun#15601).
      - name: Set up bun for publish
        if: steps.release.outputs.releases_created == 'true' || inputs.republish_paths != ''
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: .bun-version
      - name: Set up node
        if: steps.release.outputs.releases_created == 'true' || inputs.republish_paths != ''
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 24
      - name: Install dependencies
        if: steps.release.outputs.releases_created == 'true' || inputs.republish_paths != ''
        run: bun install --frozen-lockfile
      # The registry-version check makes the loop idempotent: a re-run after
      # a failed publish skips versions already on npm instead of erroring.
      - name: Publish released packages
        if: steps.release.outputs.releases_created == 'true' || inputs.republish_paths != ''
        env:
          PATHS_RELEASED: ${{ steps.release.outputs.paths_released || inputs.republish_paths }}
        run: |
          for path in $(printf '%s' "${PATHS_RELEASED:-[]}" | jq -r '.[]'); do
            name=$(jq -r .name "$path/package.json")
            version=$(jq -r .version "$path/package.json")
            if [ "$(jq -r '.private // false' "$path/package.json")" = "true" ]; then
              echo "skipping $name: private"
              continue
            fi
            if [ "$(npm view "$name@$version" version 2>/dev/null)" = "$version" ]; then
              echo "skipping $name@$version: already published"
              continue
            fi
            (cd "$path" && bun pm pack && npm publish ./*.tgz --provenance --access public)
          done
