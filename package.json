{
  "name": "@zgeoff/slooks",
  "version": "0.2.0",
  "description": "Slack hooks daemon: run shell commands on Slack events, as you",
  "license": "MIT",
  "homepage": "https://github.com/zgeoff/slooks#readme",
  "bugs": "https://github.com/zgeoff/slooks/issues",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zgeoff/slooks.git"
  },
  "type": "module",
  "bin": {
    "slooks": "bin/slooks"
  },
  "files": [
    "bin",
    "src",
    "!src/**/*.test.ts"
  ],
  "scripts": {
    "start": "bun bin/slooks daemon",
    "build:binaries": "bash scripts/build-binaries.sh",
    "audit": "bun audit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@slack/socket-mode": "^2.0.7",
    "@slack/web-api": "^8.1.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.8",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
